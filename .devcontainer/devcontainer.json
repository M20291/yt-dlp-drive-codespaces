{
  "name": "YouTube to Drive via Codespaces",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "echo 'Starting setup...' && apt-get update && apt-get install -y python3 python3-pip nodejs npm && pip3 install yt-dlp && npm install googleapis && chmod +x youtube_to_drive.sh && bash youtube_to_drive.sh",
  "remoteUser": "vscode"
}