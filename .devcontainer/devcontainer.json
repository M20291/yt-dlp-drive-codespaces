{
  "name": "YouTube Downloader",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "onCreateCommand": "apt-get update && apt-get install -y python3 python3-pip && pip3 install yt-dlp && chmod +x download_metadata.sh .devcontainer/startup.sh",
  "postCreateCommand": "bash .devcontainer/startup.sh",
  "postStartCommand": "bash .devcontainer/startup.sh"
}